# Configuração para desenvolvimento local
# Copie este arquivo para .env.local e ajuste conforme necessário

# Se true ou ausente, requer senha no primeiro acesso (comportamento de produção)
# Se false, apenas nome da equipe é necessário (facilita desenvolvimento)
NEXT_PUBLIC_REQUIRE_TEAM_PASSWORD=false

# Senha padrão quando REQUIRE_TEAM_PASSWORD=false
# Em produção, esta variável não deve ser definida
NEXT_PUBLIC_TEAM_PASSWORD=IAMASTERLAB
